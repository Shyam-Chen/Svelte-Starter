describe('Load', () => {

  beforeEach(() => {
    // jasmine.Ajax.install();
  });

  afterEach(() => {
    // jasmine.Ajax.uninstall();
  });

  it('toBe', () => {
    expect(true).toBe(true);
  });

});
