// import { load } from './load';

describe('Load', () => {

  it('not toThrow', () => {
    // expect(load).not.toThrow();
  });

  it('should be able to get json', () => {
    // expect(load('../assets/datas/firebase.config.json')).toBeDefined();
  });

  it('should be able to get font', () => {
    // expect(load('https://fonts.googleapis.com/css?family=Lora')).toBeDefined();
  });

});
